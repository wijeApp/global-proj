# MySQL Database Connection (Primary for JPA entities - users, employees, etc.)
spring.datasource.url=jdbc:mysql://localhost:3306/globalschema?useSSL=false&serverTimezone=UTC&createDatabaseIfNotExist=true&allowPublicKeyRetrieval=true&useUnicode=true&characterEncoding=UTF-8&authenticationPlugins=mysql_native_password
spring.datasource.username=sa
spring.datasource.password=myRoot@123
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Microsoft SQL Server Database Connection (Secondary for GL Reference Codes only)
# Configured in DatabaseConfig.java as "sqlServerDataSource" and "sqlServerJdbcTemplate"
# spring.datasource.url=jdbc:sqlserver://124.43.199.76:1497;databaseName=TasGlobalDB;encrypt=false;trustServerCertificate=true
# spring.datasource.username=sa
# spring.datasource.password=m@18953789
# spring.datasource.driver-class-name=com.microsoft.sqlserver.jdbc.SQLServerDriver

# JPA/Hibernate Settings (For primary MySQL datasource)
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
# Note: SQL Server connection for GL Reference Codes is handled separately in DatabaseConfig

# SQL Initialization - Re-enabled for MySQL schema creation
spring.sql.init.mode=always
spring.sql.init.continue-on-error=true
spring.jpa.defer-datasource-initialization=true

spring.application.name=globalven

# File Upload Configuration
spring.servlet.multipart.max-file-size=5MB
spring.servlet.multipart.max-request-size=5MB
app.upload.dir=uploads
